# Note: the command **/ seems to ignore the concerned file or directory anywhere in the project
# Note: ending a command line by / specifies to git that we are untracking an entire folder

# Untrack files and directories generated by cmake
**/CMakeFiles/
**/Makefile
**/CMakeCache.txt
**/*.cmake

# Untrack project files (don't know yet what they are related to but likely to Eclipse)
**/*.project
**/*.cproject
